{
  "name": "medical-store-pro",
  "version": "1.0.0",
  "description": "Standard Medical Store Project Structure",
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix medical-shop\"",
    "dev": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix medical-shop\"",
    "install:all": "npm install --prefix backend && npm install --prefix medical-shop"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
